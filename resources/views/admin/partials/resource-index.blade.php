<div class="flex justify-between items-center mb-8">
    <h1 class="text-2xl font-semibold text-navy-900">{{ $title }}</h1>
    <a href="{{ $createRoute }}" class="px-4 py-2 bg-navy-900 text-white text-sm font-semibold rounded-lg hover:bg-navy-800">Add New</a>
</div>
